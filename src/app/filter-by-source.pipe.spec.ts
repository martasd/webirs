import { FilterBySourcePipe } from './filter-by-source.pipe';

describe('FilterBySourcePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBySourcePipe();
    expect(pipe).toBeTruthy();
  });
});
