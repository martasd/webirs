import { FilterByNodePipe } from './filter-by-node.pipe';

describe('FilterByNodePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByNodePipe();
    expect(pipe).toBeTruthy();
  });
});
